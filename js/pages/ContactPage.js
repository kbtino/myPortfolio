/**
 * ContactPage.js — Page Contact (#/contact)
 * Implémentée en v0.9.0
 */

export const ContactPage = {
  render(container) {
    container.innerHTML = `
      <section class="page page--contact">
        <p class="page__placeholder">Contact — v0.9.0</p>
      </section>
    `;
  },
};
