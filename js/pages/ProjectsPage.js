/**
 * ProjectsPage.js — Page Projets (#/projects)
 * Implémentée en v0.6.0
 */

export const ProjectsPage = {
  render(container) {
    container.innerHTML = `
      <section class="page page--projects">
        <p class="page__placeholder">Projets — v0.6.0</p>
      </section>
    `;
  },
};
