/**
 * animations.js — Animations déclenchées au scroll (IntersectionObserver)
 * Implémenté en v1.0.0
 */

export const animations = {
  init() {
    // v1.0.0 : IntersectionObserver sur les éléments .animate-on-scroll
  },
};
